java中被static修饰的成员为 静态成员---->它是属于整个类所有。


静态变量是所有的类实例共享此静态变量===注意这里的共享
	在类第一次被使用时装载,只分配一块存储空间,所以此类的对象都可以操控此块存储空间
	
	首先：static定义的变量会优先于任何其他非static变量,不论出现的顺序如何。
		静态方法也是;只会初始化一次,而且是在类被第一次装载的时候。
		
	-----------------------------------------------------------
	该包内的代码要说明一个问题。
	就是我的类MapContains中有一个静态变量对象 v。
	当并发时：线程A对v进行操作时,线程B也要读取该对象数据。如何做到同步呢？
	如果线程都是一个共享对象MapContains时。还好说，我们直接在MapContains的方法上加上
	sychronized 关键字
	但是如果是不同的对象MapContains时,那么这时他们共享的内容确是这个静态变量对象v
	那么我们就那这个静态变量为锁,同步代码块
	sychronized(v){
	
	}
	
	具体看代码。
	